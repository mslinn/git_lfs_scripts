# Test Scripts

The shell scripts in this subdirectory use the commands in `../bin/` to
perform the tests as described in `https://mslinn.com/git/index.html`
under the section: "Git Large File System".
